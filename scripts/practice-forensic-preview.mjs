import {readFileSync,writeFileSync} from 'node:fs';
import {join} from 'node:path';
import {createRequire} from 'node:module';
import ts from 'typescript';
import React from 'react';
import {renderToStaticMarkup} from 'react-dom/server';
const directory=process.argv[2];
const require=createRequire(import.meta.url);
const source=readFileSync(new URL('../app/components/PracticeRecap.tsx',import.meta.url),'utf8');
const compiled=ts.transpileModule(source,{compilerOptions:{module:ts.ModuleKind.CommonJS,jsx:ts.JsxEmit.ReactJSX}}).outputText;
const compiledModule={exports:{}};
new Function('require','module','exports',compiled)(name=>name.endsWith('.css')?{default:new Proxy({},{get:(_,key)=>String(key)})}:require(name),compiledModule,compiledModule.exports);
const summary=JSON.parse(readFileSync(join(directory,'analytics-reconciliation.json'),'utf8'));
const css=readFileSync(new URL('../app/components/PracticeRecap.module.css',import.meta.url),'utf8');
const markup=renderToStaticMarkup(React.createElement(compiledModule.exports.PracticeRecap,{summary}));
writeFileSync(join(directory,'practice-review.html'),`<!doctype html><html lang="en"><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>Private Practice Review QA</title><style>:root{--text:#151515;--muted:#555;--line:#bbb;color-scheme:light dark}body{margin:0;background:#fff;color:var(--text);font:16px system-ui}main{max-width:1000px;margin:auto;padding:20px}h1{font-size:24px}@media(prefers-color-scheme:dark){:root{--text:#f4f4f4;--muted:#aaa;--line:#444}body{background:#121212}}${css}</style><main><h1>Practice Complete</h1><p>September 17 · Read-only QA</p>${markup}</main></html>`);
console.log('Private preview rendered from the actual PracticeRecap component and actual Analytics result.');
